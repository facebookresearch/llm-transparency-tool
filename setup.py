from setuptools import setup

# TODO(igortufanov): keep only llm_transparency tool when that repo forks out
setup(
    name="transparent_llms",
    version="0.1",
    packages=["llm_transparency_tool"],
)
